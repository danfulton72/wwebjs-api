search_contacts:
  fields:
    session_id:
      required: true
      example: "default"
      selector:
        text:
    pattern:
      required: true
      example: "(?i)alice|447700900123"
      selector:
        text:
