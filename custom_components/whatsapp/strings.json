{
  "config": {
    "step": {
      "user": {
        "title": "Connect WhatsApp HA",
        "description": "Connect WhatsApp HA to your WWebJS API server. Sessions will be discovered automatically.",
        "data": {
          "url": "Server URL",
          "api_key": "API key"
        },
        "data_description": {
          "url": "Base URL reachable from Home Assistant, for example http://192.168.1.20:3000.",
          "api_key": "The API_KEY configured on the WWebJS API server."
        }
      },
      "reconfigure": {
        "title": "Reconfigure WhatsApp HA",
        "description": "Update the server URL or API key. The connection is tested before the entry is saved.",
        "data": {
          "url": "Server URL",
          "api_key": "API key"
        }
      },
      "reauth_confirm": {
        "title": "Re-authenticate WhatsApp HA",
        "description": "The saved API key was rejected. Enter the current API key for the WWebJS API server.",
        "data": {
          "api_key": "API key"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to the WWebJS API server",
      "invalid_auth": "Invalid API key"
    },
    "abort": {
      "already_configured": "This WWebJS API server is already configured",
      "cannot_connect": "The YAML configuration could not be imported because the WWebJS API server could not be reached",
      "invalid_auth": "The YAML configuration could not be imported because the API key was rejected",
      "reauth_successful": "The WhatsApp HA API key was updated successfully",
      "reconfigure_successful": "The WhatsApp HA connection was updated successfully"
    }
  },
  "services": {
    "search_contacts": {
      "name": "Search contacts",
      "description": "Return contacts from a WhatsApp session whose identity fields match a Python regular expression. The WWebJS API connection is selected automatically from the configured WhatsApp HA integration. Every JSON attribute supplied by the contacts API is preserved on each matching contact, including nested ID data, booleans, labels, names, number and future fields.",
      "fields": {
        "session_id": {
          "name": "Session ID",
          "description": "The WWebJS session to search, for example default or personal. The integration automatically uses the configured connection that owns this session."
        },
        "pattern": {
          "name": "Regular expression",
          "description": "Python regular expression matched against name, pushname, shortName, verifiedName, number, and contact ID fields. Use (?i) for case-insensitive matching. Matching contacts are returned with all API attributes unchanged."
        }
      }
    }
  },
  "exceptions": {
    "no_loaded_connection": {
      "message": "No loaded WhatsApp HA connection is available."
    },
    "ambiguous_session_connection": {
      "message": "Session {session_id} exists on more than one WhatsApp HA connection, so the connection cannot be selected automatically."
    },
    "session_connection_not_found": {
      "message": "Session {session_id} was not found on any loaded WhatsApp HA connection."
    },
    "invalid_regex": {
      "message": "The regular expression is invalid: {error}"
    },
    "contact_search_failed": {
      "message": "The contacts request failed: {error}"
    }
  },
  "entity": {
    "sensor": {
      "sessions": {
        "name": "Sessions"
      },
      "status": {
        "name": "Status"
      }
    },
    "camera": {
      "pairing_qr": {
        "name": "Pairing QR code"
      }
    }
  }
}
