# Workflow policy

Every third-party `uses:` reference in this directory must be pinned to a full 40-character commit SHA. The `Pinned Actions Check` workflow enforces this on pull requests.

Mutable major/minor tags may be left only in comments for human-readable provenance. Dependabot proposes reviewed pin updates.
